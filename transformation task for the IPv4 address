#include <stdio.h>
int main() {
    unsigned char a,b,c,d;
    scanf("%hhu.%hhu.%hhu.%hhu",&a,&b,&c,&d);
    unsigned int result = (a << 24) | (b << 16) | (c << 8) | d;
    printf("%u", result);
    return 0;
}
//the point of solution is to move the binary string respectively.However,it still turns out that I'm a green hand while I don't know the Format specifier of strings.
https://en.cppreference.com/w/c/io/fprintf
//the table in the web shows the format specifiers.
